#!/usr/bin/env python3
import os
import sys

_here = os.path.dirname(os.path.realpath(__file__))
sys.path.insert(0, os.path.join(_here, "..", "src"))

from sievebox.cli import main  # noqa: E402

sys.exit(main())
